databases:
  - name: life-insurance-crm-db
    plan: free
    databaseName: crm
    user: crm

services:
  - type: web
    name: life-insurance-crm
    env: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: uvicorn backend.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: life-insurance-crm-db
          property: connectionString
      - key: GMAIL_USER
        value: samirawad24@gmail.com
      - key: NOTIFY_EMAIL
        value: samirawad24@gmail.com
      - key: GMAIL_APP_PASSWORD
        sync: false
      - key: TELEGRAM_CHAT_ID
        value: "6092227774"
      - key: TELEGRAM_BOT_TOKEN
        sync: false
